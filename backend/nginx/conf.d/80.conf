server {
    listen 80;
    server_name api.mindtype.cn;

    # Let's Encrypt ACME 校验目录（certbot webroot 模式写入）
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 其余请求全部 301 跳转到 HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}
